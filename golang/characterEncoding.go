package main

func characterEncoding(corpus string) ([]int, error) {

	// To implement this, we would have to do the following
	/* Step # 01: Get all the unique characters
	   Step # 02: Incremently tokenize them that is assign numerical values
	   Step # 03: Return the entire slice of integer
	*/
	stoi := make(map[string]int)
	itos := make(map[int]string)

	// Iterating over all the
	return nil, nil
}
